# App Environment
# Use "development" for local dev, "production" for deployment, etc.
ENVIRONMENT=

# -------------------------------
# PostgreSQL Database Connection
# -------------------------------

# PostgreSQL hostname (e.g., db, localhost, or production DB host)
POSTGRES_HOST=

# PostgreSQL port (default 5432)
POSTGRES_PORT=

# PostgreSQL credentials
POSTGRES_USERNAME=
POSTGRES_PASSWORD=

# PostgreSQL database name
POSTGRES_DATABASE=

# Full connection string used by Prisma (constructed from above or provided directly)
# Example: postgresql://user:pass@host:port/dbname?schema=public
DATABASE_URL=

# -------------------------------
# NestJS Application Settings
# -------------------------------

# Port where the NestJS app runs
PORT=

# -------------------------------
# JWT Authentication Secrets
# -------------------------------

# You can generate secrets using:
# openssl rand -hex 32
# or by running the script: node scripts/generate_secret.js

# Secret used to sign access tokens
JWT_ACCESS_SECRET=

# Duration of access tokens (e.g., 1d, 30m)
JWT_ACCESS_EXPIRES_IN=

# Secret used to sign refresh tokens
JWT_REFRESH_SECRET=

# Duration of refresh tokens (e.g., 7d, 1h)
JWT_REFRESH_EXPIRES_IN=
